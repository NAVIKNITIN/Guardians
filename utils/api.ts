/**
 * Legacy re-exports — new code should use `@/src/api/axiosInstance` and
 * `@/src/api/services/*` instead of the previous `ApiClient` wrapper.
 */
export { API_BASE_URL } from "@/src/api/config";
export { axiosInstance } from "@/src/api/axiosInstance";
